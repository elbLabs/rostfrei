# `Command`

One JSON path for commands. Commands carry boundary payload metadata and an exact JSON codec. The handler supplies aggregate and rejection relationships. Define only the input fields needed by the operation.

- Schema version 1 by default
- Exact JSON shape
- Owner-independent payload

**Placement:** [Aggregate action](/docs/project-structure/aggregate-action) — `command.rs`.

<MacroExample name="Command" />
