# `domain_model!`

Keep model composition visible. The explicit inventory builds one deterministic catalog. Events and identities are discovered through their aggregate and entity relationships. Register the top-level domain types in one place.

- One visible composition root
- Returns validated JSON
- No hidden behavior inventory

**Placement:** [Domain root](/docs/project-structure/domain-root) — `model.rs`.

<MacroExample name="domain_model!" />
