# `Entity`

State plus an explicit identity. Entity metadata is derived locally. Owner, identity type, and identity access remain compiler-checked Rust. Implement the definition trait to make those relationships explicit.

- Named-field struct
- Identity accessor required
- Custom fields stay opaque

**Placement:** [Entity](/docs/project-structure/entity) — `entity.rs`.

<MacroExample name="Entity" />
