# `BoundedContext`

Name one domain language. A bounded context is a small marker with a stable ID and a human label. The derive keeps that boundary explicit without adding runtime behavior.

- Non-generic unit struct
- Stable context ID
- No runtime behavior

**Placement:** [Bounded context](/docs/project-structure/bounded-context) — `context.rs`.

<MacroExample name="BoundedContext" />
